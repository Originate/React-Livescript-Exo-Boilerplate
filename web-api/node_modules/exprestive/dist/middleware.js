// Generated by CoffeeScript 1.10.0
(function() {
  var BaseController, Exprestive, callsite, middleware, path;

  BaseController = require('./base_controller');

  callsite = require('callsite');

  Exprestive = require('./exprestive');

  path = require('path');

  middleware = function(options) {
    var baseDir, stack;
    stack = callsite();
    baseDir = path.dirname(stack[1].getFileName());
    return new Exprestive(baseDir, options).middlewareRouter;
  };

  middleware.BaseController = BaseController;

  module.exports = middleware;

}).call(this);
