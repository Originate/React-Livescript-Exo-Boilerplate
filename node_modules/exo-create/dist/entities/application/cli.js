// Generated by LiveScript 1.5.0
var green, inquirer, path, tmplconv, questions;
green = require('chalk').green;
inquirer = require('inquirer');
path = require('path');
tmplconv = require('tmplconv');
console.log('We are about to create a new Exosphere application!\n');
questions = [
  {
    type: 'input',
    name: 'app-name',
    message: 'Name of the application to create:',
    filter: function(input){
      return input.trim();
    },
    validate: function(input){
      return input.length > 0;
    }
  }, {
    type: 'input',
    name: 'app-description',
    message: 'Description:'
  }, {
    type: 'input',
    name: 'app-version',
    message: 'Initial version:',
    'default': '0.0.1'
  }
];
inquirer.prompt(questions).then(function(answers){
  var srcPath, targetPath;
  srcPath = path.join(__dirname, '..', '..', '..', 'templates', 'create-app');
  targetPath = answers['app-name'];
  console.log();
  return tmplconv.render(srcPath, targetPath, {
    data: answers
  }).then(function(){
    return console.log(green("\ndone"));
  });
});