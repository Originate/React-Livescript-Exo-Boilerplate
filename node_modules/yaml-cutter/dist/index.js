// Generated by LiveScript 1.5.0
var fs, path, YamlHashInserter, insertHash;
fs = require('fs');
path = require('path');
YamlHashInserter = require('./yaml-hash-inserter');
insertHash = function(arg$, done){
  var file, root, key, value;
  file = arg$.file, root = arg$.root, key = arg$.key, value = arg$.value;
  return fs.readFile(file, function(err, text){
    var inserter;
    switch (false) {
    case !err:
      return done(new Error("Cannot open the given YAML file '" + file + "'! " + err.message));
    }
    inserter = new YamlHashInserter(text.toString());
    inserter.insertHash({
      root: root,
      key: key,
      value: value
    });
    return fs.writeFile(file, inserter.toString(), done);
  });
};
module.exports = {
  insertHash: insertHash
};